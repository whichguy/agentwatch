name: AgentWatch
# File-targeted agent monitoring system
# Usage: @agent-watch <file|*> <agent> <args> in PR or file comments
# Commands:
#   @agent-watch <file|*> <agent> <args> - Start watching file(s)
#   @agent-unwatch <file|*> - Stop watching file(s) (* = all files)
#   @agent-list - Show all currently watched files

on:
  pull_request_review_comment:
    types: [created]
  issue_comment:
    types: [created]
  pull_request:
    types: [opened, synchronize]

permissions:
  contents: read
  issues: write
  pull-requests: write

jobs:
  agentwatch:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '18'

      - name: Install dependencies
        run: |
          npm init -y
          npm install @octokit/rest

      - name: Run AgentWatch
        uses: actions/github-script@v7
        with:
          script: |
            const { handleAgentWatch } = require('./.github/scripts/agentwatch.js');
            await handleAgentWatch(context, github);